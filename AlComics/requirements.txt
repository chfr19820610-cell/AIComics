# AIComics v2.0 — Python Dependencies
# ======================================
# Core
httpx>=0.27.0
tqdm>=4.66.0

# TTS (Chinese narration)
edge-tts>=6.1.0

# Image processing
Pillow>=10.0.0

# ffmpeg bundle (fallback when system ffmpeg unavailable)
imageio-ffmpeg>=0.5.0

# API utilities
aiohttp>=3.9.0

# ComfyUI client
websocket-client>=1.7.0

# ── Backend API ──────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
aiosqlite>=0.20.0
python-jose[cryptography]>=3.3.0
bcrypt>=4.2.0
pydantic>=2.10.0
websockets>=14.0
